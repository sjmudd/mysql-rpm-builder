# Fixture reproducing config.yaml's real quirks for MergeBuild tests:
# mixed comment-indent styles (flush-left "#" vs indented "  #"), a section
# with only commented-out entries, and a real entry followed by a
# section-divider comment before the next OS.
oses:
  trailing1:
    builds:
      # Explanatory comment attached to a disabled entry.
#      tree:
#        srpm: https://example.invalid/tree.src.rpm
#        auto_install_dependencies: true

  #    8.4.10:
  #      srpm: https://example.invalid/8.4.10.src.rpm
  #      auto_install_dependencies: true

      9.7.1:
        srpm: https://example.invalid/9.7.1.src.rpm
        packages: [cmake, gcc]

  trailing2:
    builds:
      8.4.6:
        srpm: https://example.invalid/8.4.6.src.rpm
        auto_install_dependencies: true
      8.4.7:
        srpm: https://example.invalid/8.4.7.src.rpm
        auto_install_dependencies: true

#      9.4.0:
#        srpm: https://example.invalid/9.4.0.src.rpm
#        auto_install_dependencies: false
      9.7.1:
        srpm: https://example.invalid/9.7.1-t2.src.rpm
        auto_install_dependencies: true
  #
  # Next section
  #

  allcommented:
    builds:
#      9.5.0:
#        srpm: https://example.invalid/9.5.0.src.rpm
#        packages: [cmake, gcc]

  existing:
    builds:
      9.9.9:
        srpm: https://example.invalid/9.9.9.src.rpm
        auto_install_dependencies: true
